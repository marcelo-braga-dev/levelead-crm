# CRM Kanban Outbound — Plano de Implementação (MVP Robusto)

## Contexto

O repositório está vazio (greenfield). A especificação funcional define um CRM Kanban para call center outbound com 8 etapas de funil, card único com histórico cronológico, follow-up, distribuição de leads, regras de SLA, painel gerencial e auditoria imutável. A spec original tem lacunas reais (transições faltantes, regras de comissão, RBAC, storage de anexos, etc.) e, além disso, os leads serão alimentados por uma base rica de dados de CNPJ (importação CSV: razão social, sócios, CNAE, faturamento estimado, dívidas etc.) que precisa ser modelada como entidade própria, separada do funil de vendas. Para que a plataforma seja "profissional, robusta, precisa e alavancável" — e não apenas um CRUD — este plano incorpora também pesquisa de mercado sobre operação de call center (CTI, omnichannel, métricas, gamificação), compliance legal brasileiro (LGPD/ANATEL) e frameworks de lead scoring B2B, decidindo o que entra no MVP e o que fica para fases seguintes.

Decisões já confirmadas com o usuário:
- **Stack**: Laravel + Inertia.js + React + MUI + MySQL.
- **Deploy**: VPS próprio (não gerenciado).
- **Tenancy**: single-tenant (uma empresa), mas o schema deixa espaço para multi-tenant futuro sem migração destrutiva.
- **Escopo da v1**: MVP robusto — Kanban completo, histórico unificado, follow-up, distribuição de leads, auditoria básica, SLA, modelo Company/Lead, compliance operacional básico e lead scoring (fit score). Dashboard analítico rico, gamificação, integrações externas de telefonia/WhatsApp (CTI) e API pública ficam para Fase 2, mas o modelo de dados já é desenhado para suportá-las sem retrabalho.
- **CPF de sócios**: armazenar completo quando disponível na importação (decisão do usuário) — exige coluna criptografada em repouso (`encrypted` cast do Laravel) e acesso restrito a admin/manager, com leitura registrada em `audit_logs`.
- **Validação de janela de ligação ANATEL / empresa baixada**: avisar e exigir confirmação, nunca bloquear o registro.
- **Reimportação de company com lead anterior em Won/Lost**: nunca cria novo lead automaticamente — apenas sinaliza como candidata a reciclagem numa tela dedicada; reabertura é sempre ação manual via `RecycleLeadAction`.
- **Importação de CSV**: deve ser tolerante a mudança de nome/ordem de colunas — o sistema tenta auto-detectar pelas colunas conhecidas e, quando não casar, oferece tela de mapeamento manual (usuário relaciona coluna da planilha → campo esperado pelo sistema), salvável como perfil reutilizável.

## Decisões de arquitetura para as lacunas da spec original

1. **Lead Novo → Perdido**: adicionado ao grafo de transições (cobre motivo "Dados inválidos").
2. **Retrocesso de etapa**: permitido apenas 1 nível (ex.: Qualificado → Contato Realizado), exige motivo obrigatório, gera `stage_regressed` no audit log.
3. **Reciclagem de lead perdido**: ação separada (`RecycleLeadAction`), nunca automática — cria um **novo** lead em "Lead Novo" linkado via `recycled_from_lead_id`, apontando para a mesma `company_id`. O lead perdido original permanece imutável.
4. **Comissão no Ganho**: calculada automaticamente por `CommissionResolver` (regra por produto+consultor > produto > consultor > default), editável manualmente; overrides registrados no audit log.
5. **Transferência de consultor**: ação dedicada (`TransferLeadAction`), gera registro em `lead_assignments` + entrada no histórico unificado.
6. **SLA (24h sem interação / 15d em negociação / 7d sem retorno)**: job agendado avalia e cria `sla_alerts` idempotentes; notifica o gestor via e-mail + notificação in-app. MVP só alerta, não redistribui automaticamente.
7. **RBAC**: 3 papéis (admin, manager, consultant) via coluna enum em `users`. Consultor só vê leads próprios/da equipe; manager vê tudo da empresa.
8. **Anexos de proposta**: disco `local` do VPS, abstraído via Laravel Filesystem para trocar para S3 na Fase 2 via config. Versionamento: nova proposta = nova linha (`version+1`), anterior marcada `superseded`.
9. **Fórmulas**: Taxa de Conversão = `won / (total - new)` no período; Ticket Médio = `sum(won_value) / count(won)`. Validado contra prática de mercado — segmentável por período, etapa, origem, consultor e CNAE.

## Modelo Company × Lead (decisão estrutural central)

Separar **Company** (dado cadastral/enriquecimento, importado de CSV de CNPJ, atualizado periodicamente) de **Lead** (entidade do funil de vendas). Motivo: o mesmo CNPJ pode gerar mais de um lead ao longo do tempo (reciclagem), e dados cadastrais são reimportados/atualizados sem que isso deva sobrescrever o que o consultor já validou em campo.

`leads.company_id` (FK `restrict`, not null) substitui os campos cadastrais fixos que hoje estariam embutidos no lead. O lead passa a ter campos de **contato de trabalho próprios** (`contact_name`, `contact_phone`, `contact_whatsapp`, `contact_email`), copiados de `company_contacts` na criação mas editáveis depois sem afetar o cadastro da company.

### Tabelas de lookup (seed a partir de tabelas oficiais — IBGE/Receita Federal)
- `states` (uf, name, ibge_code)
- `cities` (ibge_code, name, state_id)
- `legal_natures` (code, description) — Natureza Jurídica
- `cnaes` (code, description) — CNAE 2.3
- `partner_qualifications` (code, description)

### Tabela mestre
```
companies
  id, cnpj (unique), razao_social, nome_fantasia,
  address_type, logradouro, numero, complemento, bairro, city_id (FK), state_id (FK, denormalizado p/ filtro rápido), cep,
  matriz_filial (enum), ente_federativo (nullable),
  primary_cnae_id (FK cnaes), legal_nature_id (FK legal_natures),
  data_inicio_atividade, company_size (enum: MEI/ME/EPP/Médio/Grande),
  share_capital, is_mei, mei_entry_date, mei_exit_date,
  registration_status (enum: ativa/suspensa/inapta/baixada), registration_status_date,
  tax_regime (enum: simples/mei/presumido/real) — snapshot atual,
  estimated_revenue_value (nullable), employee_count (nullable) — snapshot atual,
  active_federal_debt, total_debt,
  site, data_provider, license_reference, last_import_batch_id (FK), last_enriched_at,
  SoftDeletes, timestamps
```

### Satélites (1:N)
- `company_secondary_activities` (company_id, cnae_id)
- `company_partners` (company_id, identificador, nome, cpf (encrypted), tipo_documento, faixa_etaria, partner_qualification_id FK, data_entrada) — **CPF armazenado criptografado em repouso, leitura restrita a admin/manager e auditada**
- `company_special_programs` (company_id, program_name)
- `company_contacts` (company_id, type enum phone/whatsapp/email/website, value, is_primary)
- `company_financial_snapshots` (company_id, snapshot_date, revenue_value, employee_count, debt_value, import_batch_id) — insert só quando o valor mudou desde o último snapshot
- `company_registration_status_history` (company_id, status, changed_at, import_batch_id)
- `company_tax_regime_history` (company_id, regime, changed_at, import_batch_id)

### Importação flexível de CSV
- `import_profiles` (id, name, created_by) — perfil de mapeamento reutilizável
- `import_profile_column_mappings` (import_profile_id, source_column_label, target_field) — relaciona cabeçalho da planilha → campo do sistema
- `import_batches` (id, import_profile_id FK, file_name, data_provider, legal_basis (enum, default `legitimate_interest`), license_reference, total_rows, created_rows, updated_rows, skipped_rows, status, started_at, finished_at)
- `import_batch_errors` (import_batch_id, row_number, raw_data json, error_message) — feedback de linhas que falharam

**Fluxo de importação**: usuário faz upload → sistema lê o cabeçalho → tenta auto-match contra aliases conhecidos dos campos (ignorando acentuação/maiúsculas, ex. "Razão" ≈ "Raz%o" do CSV mal codificado) → exibe tela de confirmação/ajuste de mapeamento (drag/dropdown coluna → campo) → usuário confirma e opcionalmente salva como `import_profile` novo → dispara `ProcessCsvImportJob` (queued, processa em lote) usando o mapeamento.

Por linha: dedupe por `cnpj` → upsert em `companies` → insere em `company_financial_snapshots`/`status_history`/`tax_regime_history` somente se mudou → **nunca cria Lead automaticamente se já existe lead para aquela company em qualquer estágio**: se não há lead nenhum, cria um em "Lead Novo"; se há lead aberto (não-terminal), só atualiza a company; se o único lead é Won/Lost, marca a company como candidata a reciclagem (visível na tela "Companies sem lead ativo") e não cria nada — reabertura é decisão manual.

## Modelo de dados — CRM (Kanban/funil)

- `users` (role enum: admin/manager/consultant, team_id nullable)
- `teams`
- `lead_sources` (Landing Page, Meta Ads, Google Ads, CSV, API, Manual — `metadata` json reservado p/ Fase 2)
- `products`
- `commission_rules` (product_id nullable, user_id nullable, percent, priority)
- `leads` — `company_id` (FK), `stage` (enum), `assigned_to`, `team_id`, `contact_name/phone/whatsapp/email`, `interest_level`, `purchase_potential`, `qualification_notes`, `loss_reason`/`loss_notes`, `won_value`/`won_product_id`/`won_commission_value`, `is_recycled`/`recycled_from_lead_id`, `last_interaction_at`, `stage_entered_at`, `contact_attempts_count`, `fit_score`, `intent_score`, `total_score`, `score_updated_at`, `archived_at`, `external_ref`, `SoftDeletes`. Índices em `(company_id, stage)`.
- `lead_stage_history` (append-only: `from_stage`, `to_stage`, `changed_by`, `reason`)
- `lead_interactions` (**histórico único do card**): `type` (categoria do evento: call/whatsapp/email/visit/note/stage_change/follow_up/system), `channel` (nullable, preenchido quando `type` é evento de comunicação), `direction` (inbound/outbound, nullable), `description`, `occurred_at`, e campos já preparados para CTI/Omnichannel da Fase 2 sem migração futura: `duration_seconds`, `hold_time_seconds`, `recording_url`, `outcome_code`, `phone_dialed`, `external_provider_ref`, `channel_metadata` (json).
- `proposals` (versionamento) + `proposal_attachments`
- `follow_ups`
- `sla_alerts` (`type`: no_interaction_24h/negotiation_15d/no_return_7d/attempts_exhausted — idempotente por `resolved_at IS NULL`)
- `lead_distribution_rules` (strategy: manual/round_robin/by_team/by_region/by_state/by_product; usa `companies.state_id`/`city_id` via `company_id` do lead)
- `lead_assignments`, `round_robin_cursors`
- `audit_logs` (genérico, append-only, actor polimórfico user/system/import — ver seção "Auditoria e versionamento de dados")
- `opt_outs` (company_id nullable, phone nullable, email nullable, reason, requested_at, requested_via, created_by) — checado antes de registrar `lead_interactions` de `channel IN (call, whatsapp)`
- `national_holidays` (date, name) — seed simples, usado por `CallWindowValidator`
- `lead_scoring_rules` (criterion, criterion_value, score_weight, is_active) — pesos configuráveis do fit score
- `loss_reason_recycle_rules` (loss_reason, suggested_recycle_days_min/max, is_recyclable, notes)

Regras de integridade: FKs de `leads`/`companies` para tabelas de histórico usam `restrict` (nunca `cascade`).

## Auditoria e versionamento de dados (Leads, Companies, Proposals, Follow-ups)

Requisito: toda modificação em `leads`, `companies`, `proposals` e `follow_ups` precisa ficar registrada de forma a permitir (a) reconstruir o estado do registro em um ponto no tempo passado (somente leitura — não é "restore"/"undo"), (b) ver quem fez a mudança e (c) ver a origem do dado (usuário via UI, job de importação CSV, comando agendado). Isso é **diferente** do `lead_interactions` (histórico único do card, voltado ao consultor, conforme spec original) — esta é uma trilha técnica campo-a-campo, **visível apenas para admin/manager**.

### Schema

`audit_logs` (com actor polimórfico e suporte a reconstrução):
```
audit_logs
  id, auditable_type, auditable_id (índice composto),
  action (enum: created/updated/deleted),
  old_values (json), new_values (json),     -- apenas campos que mudaram (diff), não o registro inteiro
  actor_type (enum: user/system/import),
  actor_id (nullable — id do user OU do import_batch_id),
  actor_label (string, denormalizado: nome do usuário ou "Importação CSV #123" ou nome do Job/Command) — resiliente a exclusão futura do ator,
  ip_address (nullable, só quando actor_type=user),
  created_at  -- append-only, sem updated_at; sem soft delete: nunca é apagado
```

Índice em `(auditable_type, auditable_id, created_at)` para reconstrução eficiente. FK de `audit_logs` para qualquer tabela: nenhuma (fica solto de propósito — não pode ser bloqueado por `restrict` nem apagado em cascata; sobrevive mesmo que o registro auditado seja soft-deleted).

### Captura automática — trait `Auditable`

`app/Concerns/Auditable.php`: trait aplicada aos models `Lead`, `Company`, `Proposal`, `FollowUp`. Usa os eventos do Eloquent (`created`, `updated`, `deleted`) — não os Events de domínio existentes (`LeadStageChanged` etc.) — para garantir que **nenhum caminho de escrita escape da auditoria**, incluindo updates feitos por jobs/comandos que não disparam Events de domínio (ex.: `ProcessCsvImportJob` fazendo upsert em `companies`). Em `updated`, grava só `getDirty()`/`getOriginal()` das colunas alteradas (não o registro inteiro) em `old_values`/`new_values`.

### Contexto do ator — `AuditContext`

`app/DomainServices/AuditContext.php`: serviço com `actingAs(string $type, ?int $id, string $label)` chamado:
- Por padrão (requisições HTTP): `actor_type=user`, `actor_id=auth()->id()`, `actor_label=auth()->user()->name`. Resolvido automaticamente, sem chamada explícita.
- No início de `ProcessCsvImportJob` (antes de processar as linhas): `AuditContext::actingAs('import', $importBatch->id, "Importação CSV #{$importBatch->id} ({$importBatch->file_name})")` — assim toda alteração em `companies` durante aquele job fica marcada com a origem exata da planilha.
- Em comandos agendados sem usuário (`RecalculateLeadScoresCommand`, `ArchiveStaleLeadsCommand` etc.): `actor_type=system`, `actor_label=` nome da classe do Command/Job.

A trait `Auditable` lê o contexto ativo de `AuditContext` no momento da escrita para preencher `actor_*`.

### Reconstrução de estado passado (somente leitura)

`app/DomainServices/AuditLogReconstructor.php`: método `stateAt(Model $entity, Carbon $pointInTime): array` — parte do snapshot mais antigo (`action=created`, `new_values`) e aplica em ordem cronológica todos os diffs (`new_values` de cada `updated`) com `created_at <= $pointInTime`, retornando o array de atributos reconstruído. **Não escreve nada** — é só leitura/exibição (não existe `RestoreAction` nem botão de "reverter"). Usado pela UI de auditoria para renderizar "como estava este Lead/Company/Proposal/Follow-up em DD/MM/AAAA HH:mm".

### Acesso restrito

`AuditLogPolicy`: somente `admin`/`manager` podem listar `audit_logs` ou acessar a tela de reconstrução — consultor não tem acesso, mesmo para os próprios leads (ele continua vendo só o `lead_interactions`/histórico único do card, que é a UI já prevista na spec original).

### UI (admin/manager apenas)

`resources/js/Pages/Admin/Audit/Index.tsx` — lista filtrável por entidade/ator/período; `resources/js/Pages/Admin/Audit/Show.tsx` — timeline de diffs de um registro específico + seletor de data/hora que chama o reconstructor e mostra o "snapshot" daquele momento lado a lado com o estado atual.

## Camada de Compliance (LGPD/ANATEL) — o que entra no MVP

1. **`opt_outs`**: antes de qualquer `lead_interactions` de canal call/whatsapp, verifica por `company_id`, `phone` ou `email`; se houver match, exibe aviso (não bloqueia — consistente com a decisão de avisar+confirmar).
2. **Rastreabilidade de origem**: `import_batches.data_provider`/`legal_basis`/`license_reference` — captura desde a primeira importação, impossível de reconstruir depois.
3. **`CallWindowValidator`** (DomainService): valida 9h-21h dia útil / 10h-16h sábado / bloqueado domingo e feriado (via `national_holidays`) — **avisa e exige confirmação**, não bloqueia.
4. **Aviso de empresa baixada/inapta**: ao registrar interação de `type=call` para company com `registration_status IN (baixada, inapta)`, exibe banner de aviso no card — não bloqueia.
5. **Contador de frequência por número discado**: validação antes de nova call, baseada em `lead_interactions.phone_dialed` (não em `lead_id`, pois o mesmo número pode atravessar leads reciclados) — máx 2/dia, 15/mês — **avisa**, não bloqueia.
6. **Arquivamento de leads antigos sem resposta**: `ArchiveStaleLeadsCommand` (mensal), marca `leads.archived_at` para leads não-terminais sem interação há 12-18 meses (parametrizável). Arquivado ≠ excluído — só some do Kanban ativo por padrão.

Fica fora do sistema (responsabilidade jurídica do usuário, não código): Relatório de Impacto à Proteção de Dados (RIPD), auditoria de licitude do fornecedor de dados, documentação formal de balanceamento de legítimo interesse.

## Lead Scoring (MVP: fit score completo, intent score simplificado)

- `lead_scoring_rules`: pesos configuráveis por critério firmográfico (porte, faixa de faturamento, CNAE, estado, regime tributário) aplicados aos dados de `companies` vinculada ao lead.
- `leads.fit_score`: calculado a partir das regras acima.
- `leads.intent_score`: fórmula fixa no MVP (não configurável) — pontos por interação recente, proposta enviada/aberta, com decaimento linear por inatividade.
- `leads.total_score` = fit + intent (fórmula simples no MVP; thresholds Hot ≥80 / Warm 60-79 / Cold <60 calibrados depois com dados reais).
- `RecalculateLeadScoresJob` (diário) + recálculo incremental de `intent_score` via listener em nova `lead_interactions`.
- UI mínima: badge Hot/Warm/Cold no `LeadCard.tsx`. Gráficos/dashboard de score ficam para Fase 2.

## Reciclagem por motivo de perda

- `loss_reason_recycle_rules`: mapeia `loss_reason` → janela sugerida de reabertura (ex.: sem orçamento 45-60d, timing ruim 30-45d, concorrência 60-90d, sem interesse `is_recyclable=false`).
- `leads.contact_attempts_count`: incrementado via listener a cada interação outbound de call/whatsapp.
- Ao atingir `contact_attempts_count >= 7` sem avanço de etapa, gera `sla_alerts` (`type=attempts_exhausted`) sugerindo mover para Perdido — **sempre sugestão, nunca movimentação automática**.
- Ao perder um lead, o card exibe a sugestão de janela de reciclagem calculada a partir de `lead_stage_history` + a regra do motivo.

## Máquina de estados do Kanban

Enum PHP 8.1 backed (`LeadStage`) + array de transições permitidas em `LeadStageTransitionService`:

```
New              -> [AttemptingContact, Lost]
AttemptingContact-> [ContactMade, Lost]
ContactMade      -> [Qualified, Lost, AttemptingContact]
Qualified        -> [ProposalSent, Lost, ContactMade]
ProposalSent     -> [Negotiation, Lost, Qualified]
Negotiation      -> [Won, Lost, ProposalSent]
Won, Lost        -> [] (terminais; reciclagem é ação separada, não transição)
```

Toda transição passa **exclusivamente** por `LeadStageTransitionService` dentro de `DB::transaction` (stage change + `lead_stage_history` + `lead_interactions` + `audit_logs` atômicos).

## Arquitetura de código (Laravel)

```
app/Actions/Leads/        CreateLeadAction, AdvanceLeadStageAction, RecycleLeadAction, TransferLeadAction, AssignLeadAction
app/Actions/Proposals/    CreateProposalVersionAction
app/Actions/SlaAlerts/    EvaluateSlaForLeadAction
app/Actions/Companies/    ImportCompaniesFromCsvAction, ResolveColumnMappingAction
app/Concerns/              Auditable (trait — hooks created/updated/deleted nos models auditáveis)
app/DomainServices/       LeadStageTransitionService, LeadDistributionService, CommissionResolver, AuditContext, AuditLogReconstructor, CallWindowValidator, LeadScoringService
app/Enums/                LeadStage, LossReason, InteractionChannel, InteractionDirection, UserRole, RegistrationStatus, AuditActorType
app/Events/ + Listeners/  LeadStageChanged->RecordStageHistory, LeadAssigned, SlaBreached->NotifyManagerOnSlaBreach, LeadInteractionRecorded->IncrementContactAttempts/RecalculateIntentScore
app/Models/                Lead, LeadStageHistory, LeadInteraction, Proposal, ProposalAttachment, FollowUp, SlaAlert, AuditLog, Team, User, LeadSource, Product, CommissionRule, LeadDistributionRule, LeadAssignment, Company, CompanyPartner, CompanyContact, CompanyFinancialSnapshot, ImportBatch, ImportProfile, OptOut, LeadScoringRule, LossReasonRecycleRule
app/Policies/              LeadPolicy, ProposalPolicy, CompanyPolicy, AppendOnlyPolicy, AuditLogPolicy
app/Http/Controllers/Kanban/   LeadController, LeadStageController, LeadInteractionController, FollowUpController, ProposalController, LeadAssignmentController
app/Http/Controllers/Companies/ ImportController (upload, mapping, confirm), CompanyController (lista "sem lead ativo")
app/Http/Controllers/Dashboard/DashboardController
app/Http/Controllers/Admin/    UserController, TeamController, ProductController, LeadDistributionRuleController, CommissionRuleController, LeadScoringRuleController, AuditController (index, show com reconstrução por data)
app/Http/Requests/         StoreLeadRequest, AdvanceLeadStageRequest, LostLeadRequest, QualifyLeadRequest, StoreProposalRequest, RegisterInteractionRequest, StoreFollowUpRequest, TransferLeadRequest, ConfirmImportMappingRequest
app/Jobs/                  EvaluateLeadSlaJob, DistributeUnassignedLeadsJob, ProcessCsvImportJob, RecalculateLeadScoresJob
app/Console/Commands/      EvaluateSlaCommand, RunLeadDistributionCommand, MarkFollowUpsOverdueCommand, ArchiveStaleLeadsCommand, RecalculateLeadScoresCommand
```

Scheduler (`routes/console.php`):
```php
Schedule::command('leads:evaluate-sla')->everyFiveMinutes();
Schedule::command('leads:run-distribution')->everyFiveMinutes();
Schedule::command('follow-ups:mark-overdue')->everyMinute();
Schedule::command('leads:archive-stale')->monthly();
Schedule::command('leads:recalculate-scores')->daily();
```

## Distribuição automática de leads

`LeadDistributionService::assign()`: busca `lead_distribution_rules` ativas por prioridade → filtra pool de consultores elegíveis pela regra compatível (equipe/região via `companies.state_id`/`city_id`/produto) → aplica round-robin via `round_robin_cursors` (cursor isolado por escopo) → fallback round-robin global. Dispara `LeadAssigned` (histórico) e grava `lead_assignments`.

## Frontend (Inertia + React + MUI)

```
resources/js/Pages/Companies/Import/UploadStep.tsx, MappingStep.tsx, ConfirmStep.tsx   # wizard de importação CSV
resources/js/Pages/Companies/WithoutActiveLead.tsx        # candidatas a reciclagem
resources/js/Pages/Kanban/Board.tsx                 # colunas = stages
resources/js/Pages/Kanban/Partials/LeadCard.tsx       # badge Hot/Warm/Cold
resources/js/Pages/Kanban/Partials/LeadDetailDrawer.tsx   # MUI Drawer, abas: Dados/Empresa/Histórico/Propostas/Follow-ups
resources/js/Pages/Kanban/Partials/Forms/*.tsx
resources/js/Components/HistoryTimeline.tsx
resources/js/Components/StageAdvanceButton.tsx
resources/js/Components/ComplianceWarningModal.tsx   # aviso de janela ANATEL / empresa baixada / opt-out
resources/js/Pages/Dashboard/Index.tsx
resources/js/Pages/Admin/*/Index.tsx
resources/js/Pages/Admin/Audit/Index.tsx, Show.tsx          # trilha de auditoria + reconstrução de estado (admin/manager)
```

Drag-and-drop com `@dnd-kit/core` + `@dnd-kit/sortable`. Backend **revalida sempre** via `LeadStageTransitionService`. Autenticação: **Laravel Breeze** (Inertia/React), componentes trocados para MUI.

## Fases de implementação dentro do MVP

> Status atualizado conforme o trabalho avança — ver também `CLAUDE.md` ("Estado do projeto") para o detalhe de arquivos. Ao concluir uma fase, marcar aqui antes de encerrar a sessão, para que outra sessão/console saiba exatamente onde retomar.

1. ✅ **Concluída.** Fundação: migrations completas (incluindo `companies` e satélites, lookups geográficos/CNAE, `opt_outs`, `import_*`, `lead_scoring_rules`, `loss_reason_recycle_rules`, `audit_logs` com actor polimórfico), models, trait `Auditable` aplicada a Lead/Company/Proposal/FollowUp desde o início, seeders (states/cities/cnaes/legal_natures — **amostra de desenvolvimento**, não o fixture oficial completo; ver comentários em `database/seeders/`), Breeze, RBAC, Policies (incluindo `AuditLogPolicy`). Validado com `sail artisan migrate --seed` + 32 testes Pest + Pint sem pendências.
2. ✅ **Concluída.** Importação de Companies + CRUD de Leads + Kanban estático: wizard de importação CSV em página única (`Companies/Import.tsx` com Stepper) com auto-detecção de colunas (`ResolveColumnMappingAction`), ajuste manual e perfis salvos (`ImportProfile`/`ImportProfileColumnMapping`), `ImportCompaniesFromCsvAction` (dedupe por CNPJ, snapshots de histórico só quando muda, regra de Lead único por company) rodando dentro de `ProcessCsvImportJob` com `AuditContext::actingAs('import', ...)` (disparado via `dispatchSync` — ainda sem worker de fila no Sail), `Company::scopeWithoutActiveLead()` (tela "Companies sem lead ativo" como filtro em `Companies/Index.tsx`), `CreateLeadAction` reaproveitado pela importação e pela criação manual, Kanban estático em `Kanban/Board.tsx` (sem drag-and-drop — isso é Fase 3) com `LeadDetailDialog` para editar contato/qualificação e excluir (admin). Sócios (`company_partners`) ficaram fora do wizard de propósito — entram em importação dedicada futura. Nova convenção de frontend adotada aqui e registrada no `CLAUDE.md`: priorizar `Dialog`/`Drawer` MUI sobre páginas Inertia novas. Validado com 48 testes Pest (incluindo fluxo HTTP completo do wizard) + Pint sem pendências + verificação manual no navegador (upload com cabeçalho renomeado/reordenado, linha inválida registrada em `import_batch_errors`, RBAC do board e do delete de lead).
3. ✅ **Concluída.** Máquina de estados + drag-and-drop funcional: `LeadStageTransitionService::transition()` (grafo fechado de transições, retrocesso de 1 nível com motivo obrigatório, `Won`/`Lost` terminais) sempre dentro de `DB::transaction`, gravando `lead_stage_history` e disparando `audit_logs` via a trait `Auditable` já existente em `Lead` (sem novo valor no enum `audit_logs.action`, que é fixo). `TransitionLeadStageRequest` exige `loss_reason` para `Lost` e `won_value` para `Won` (`required_if` + `nullable`, para não colidir com o restante dos campos sempre enviados juntos pelo form do frontend). `LeadStageController::update()` em `PATCH /leads/{lead}/stage`, autorizado via `Gate::authorize('update', $lead)`. `Kanban/Board.tsx` ganhou `@dnd-kit/core` (`DndContext`/`useDraggable`/`useDroppable`); drop revalida primeiro no grafo TS (`utils/leadStage.ts`) só para UX (bloquear visualmente jump inválido e decidir qual `StageTransitionDialog` abrir — perda/ganho/retrocesso), mas o backend é que sempre decide de verdade. Histórico de etapas exibido no `LeadDetailDialog` (seção "Histórico de etapas"). Validado com 57 testes Pest (incluindo um teste que reproduz o payload real do dialog — todos os campos enviados juntos, mesmo vazios — que pegou um bug de validação que os testes HTTP "limpos" não pegavam) + Pint + `npm run build` + verificação manual no navegador via Playwright cobrindo: avanço simples, jump inválido bloqueado no cliente, retrocesso com motivo, perda com validação de `loss_reason`, ganho com `won_value`, histórico no dialog. **Bug real encontrado só na verificação de navegador** (não nos testes unitários/HTTP isolados): rotas com binding implícito exigem nome do parâmetro igual ao wildcard da rota (já documentado no item 2); e relações Eloquent serializam em `snake_case` (`Str::snake` do nome do método) — `assignedTo()`→`assigned_to`, `stageHistory()`→`stage_history`, `changedBy()`→`changed_by`, `columnMappings()`→`column_mappings` — o frontend assumia camelCase em vários lugares (`LeadCard.tsx`, `LeadDetailDialog.tsx`, `ImportMappingStep.tsx`, `Import.tsx`), causando campos undefined silenciosos. **Lição registrada**: ao adicionar `with()` de uma relação nova, sempre confirmar a chave real no JSON antes de tipar no TS. Também aproveitado para corrigir um gap geral do app: mensagens de validação do Laravel apareciam em inglês na UI (`APP_LOCALE`/`APP_FALLBACK_LOCALE` não estavam definidos) — adicionado `lang/pt_BR/validation.php` e `APP_LOCALE=pt_BR` no `.env`/`.env.example`.
4. ✅ **Concluída.** Follow-up + Propostas (upload e versionamento): `CreateProposalVersionAction` cria sempre uma nova linha em `proposals` (version+1) e marca a `active` anterior como `superseded` — nunca update/delete da versão antiga —, salva anexos via Laravel Filesystem (disco `local`, `storage/app/private/proposals/{lead_id}/{proposal_id}/...`) em `proposal_attachments`, e tudo dentro de `DB::transaction`. `CreateFollowUpAction`/`CompleteFollowUpAction` cobrem o ciclo de um `follow_up` (`pending`→`done`); concluir um follow-up já `done` lança `DomainException`. Ambas as actions de criação/conclusão geram uma entrada em `lead_interactions` (tipos `system` e `follow_up`) — fechando uma lacuna que tinha ficado da Fase 3: `LeadStageTransitionService::transition()` também passou a gravar `lead_interactions` (tipo `stage_change`) na mesma transação, então `lead_interactions` agora é de fato o "histórico único do card" que o plano sempre previu, e não só `lead_stage_history`. Novos `ProposalController`/`FollowUpController` (rotas `POST /leads/{lead}/proposals`, `GET /proposal-attachments/{attachment}/download`, `POST /leads/{lead}/follow-ups`, `PATCH /follow-ups/{followUp}/complete`), `FollowUpPolicy` (mesmo padrão de `ProposalPolicy`: delega para `LeadPolicy` via `$followUp->lead`). `LeadDetailDialog.tsx` ganhou abas (`Tabs` do MUI: Dados/Histórico/Propostas/Follow-ups) em vez de um scroll único — `ProposalsSection.tsx`/`FollowUpsSection.tsx` novos, mantendo a convenção de não criar páginas novas. Validado com 68 testes Pest + Pint + `npm run build` + verificação manual no navegador via Playwright (criar proposta com anexo PDF real, criar 2ª versão e confirmar que a 1ª virou "Substituída", baixar o anexo, agendar e concluir um follow-up). **Dois bugs reais encontrados só na verificação de navegador**: (1) `Kanban/Board.tsx` guardava o lead selecionado como objeto (`useState<LeadCardData | null>`) capturado no clique do card — após criar uma proposta/follow-up, a visita Inertia atualiza `columns` (props) mas o `state` antigo não era resssincronizado, então o diálogo continuava mostrando "Propostas (0)" mesmo com o registro já salvo no banco; corrigido guardando só o `id` selecionado e derivando o lead atual via `columns.flatMap(...).find(...)` a cada render, sempre a partir da prop mais recente. **Lição**: ao guardar "o item selecionado" de uma lista que pode ser atualizada por uma ação dentro do próprio diálogo (não só por navegação), guardar o id e derivar da prop, nunca guardar o objeto em si. (2) O teste manual inicial usava um arquivo `.pdf` que na verdade era texto puro — a regra `mimes:pdf` do Laravel valida o conteúdo real do arquivo (magic bytes via `finfo`), não a extensão, então a primeira tentativa falhou silenciosamente a validação; não era bug do app, mas reforça que testes manuais de upload precisam de um arquivo do tipo MIME real, não só do nome.
5. ✅ **Concluída.** Compliance operacional: como não existia nenhum caminho para registrar uma interação manual (call/whatsapp/email/visit/note) antes desta fase, criou-se `RegisterInteractionAction` + `LeadInteractionController::store()` (`POST /leads/{lead}/interactions`) como esse caminho — e é nele que a `ComplianceChecker` (`app/DomainServices/ComplianceChecker.php`) roda antes de salvar, agregando: opt-out por `company_id`/`phone` (`OptOut`), `CallWindowValidator` (dias de semana 9h-21h, sábado 10h-16h, domingo e feriado nacional sempre bloqueados via `national_holidays`, já seedado desde a Fase 1), aviso de empresa com `registration_status` `baixada`/`inapta`, e contador de frequência por `phone_dialed` (não por `lead_id`, igual o plano pede, pois o mesmo número pode atravessar leads reciclados) com limites sugeridos de 2/dia e 15/mês. Todas as checagens só **avisam** — se houver aviso e a request não vier `confirmed=true`, nada é salvo e os avisos voltam via `session()->flash('compliance_warnings', ...)`; o consultor decide se confirma (nunca um bloqueio). `contact_attempts_count` incrementa e `last_interaction_at` atualiza a cada interação outbound de call/whatsapp. Rastreabilidade de origem (`import_batches.data_provider`/`legal_basis`/`license_reference`) já estava coberta desde a Fase 2 — nada a fazer aqui. Arquivamento de leads antigos (`ArchiveStaleLeadsCommand`) é explicitamente da Fase 8 ("SLA + auditoria avançada"), não desta. `LeadDetailDialog.tsx`: a aba "Histórico" virou "Interações" e passou a renderizar `lead.interactions` (a timeline unificada de verdade) com o formulário de registro acima da lista, em vez do antigo `lead.stage_history` isolado — `InteractionsSection.tsx` novo; `LeadStageHistory`/`lead_stage_history` continuam existindo no banco (gravados por `LeadStageTransitionService`) só como trilha técnica, sem mais eager-load/serialização para o frontend. Validado com 80 testes Pest (incluindo testes unitários do `CallWindowValidator` para cada janela e teste HTTP completo do fluxo aviso→confirmação) + Pint + `npm run build` + verificação manual no navegador (nota salva direto mesmo fora da janela, ligação fora da janela avisa e só salva após confirmar, opt-out por telefone avisa, empresa baixada avisa). **Bug real encontrado só na verificação manual, fora do escopo direto da feature**: `config('app.timezone')` estava hardcoded em `'UTC'` em vez de `env('APP_TIMEZONE', 'UTC')` — a `.env` já tinha `APP_TIMEZONE=America/Sao_Paulo` desde a Fase 1, mas o config nunca lia essa variável, então todo `now()` da aplicação rodava 3h adiantado (UTC) desde o início do projeto. Isso ficou invisível até agora porque nenhuma feature anterior dependia da hora do dia — o `CallWindowValidator` foi o primeiro código a realmente usar `now()->hour`/dia da semana para uma decisão de negócio, e expôs o bug imediatamente. Corrigido com uma linha; suíte completa revalidada após a mudança (datas/timestamps já armazenados não foram tocados, só o comportamento de `now()` a partir daqui).
6. ✅ **Concluída.** Distribuição de leads: `LeadDistributionService::assign()` percorre `lead_distribution_rules` ativas por prioridade (pulando `manual`), monta o pool elegível por estratégia (`round_robin`=todos os consultores, `by_team`=`team_id` da regra, `by_state`=`companies.state_id` exatamente igual, `by_region`=mesma macrorregião IBGE — `region` não existe como coluna em `states`, então usa um array estático fixo `UF→região` dentro do service, sem migração nova) e escolhe via round-robin com cursor isolado em `round_robin_cursors` (`scope_type`='rule'/`scope_id`=id da regra, ou `scope_type`='global' sem regra compatível). `by_product` é estratégia não suportada e sempre pulada (pool vazio) — `Lead` não tem campo de "produto esperado" pré-venda (só `won_product_id`, preenchido só ao Ganhar), não há o que casar; documentado aqui em vez de implementado, análogo a como o plano já trata `by_cnae` como decisão aberta. `AssignLeadAction` cobre atribuição manual E transferência com o mesmo código (corpo idêntico: atualizar `assigned_to`/`team_id`, criar `LeadAssignment` com `assigned_by`, logar em `lead_interactions`) — o plano previa duas Actions separadas, mas a duplicação não se justificava. `DistributeUnassignedLeadsJob` processa leads `assigned_to=null`, não-arquivados, não-terminais; **propositalmente não implementa `ShouldQueue`** (ver bug abaixo). `RunLeadDistributionCommand` (`leads:run-distribution`) agendado via `Schedule::command(...)->everyFiveMinutes()` direto em `routes/console.php` (padrão do Laravel 13, sem `withSchedule()` em `bootstrap/app.php`). `LeadPolicy::assign()` restringe a admin/manager. `LeadAssignmentController` expõe `POST /leads/{lead}/assignment` (atribuição/transferência manual) e `POST /leads/distribute` (distribuição em massa sob demanda, mesmo padrão `dispatchSync()` do `ProcessCsvImportJob` já usado na Fase 2 — sem worker de fila no Sail). Frontend: select de "Consultor responsável" na aba Dados do `LeadDetailDialog` (visível só para admin/manager) e botão "Distribuir leads sem consultor" no topo do `Kanban/Board.tsx`. Validado com 91 testes Pest (round_robin, by_team, by_state, by_region, prioridade entre regras, fallback global, regra manual sempre pulada, RBAC de atribuição e de distribuição em massa) + Pint + `npm run build` + verificação manual no navegador via Playwright (distribuir 3 leads sem consultor e confirmar rotação round-robin, atribuir manualmente, transferir para outro consultor, confirmar as duas entradas no histórico unificado de interações). **Bug real de framework encontrado só na verificação manual**: `dispatchSync()` numa classe que implementa `ShouldQueue` executa o `handle()` corretamente (a mutação no banco acontece), mas **descarta o valor de retorno** — confirmado isolando a chamada (`handle()` direto retornava `1`, mas `dispatchSync()` da mesma classe `ShouldQueue` retornava `0`, mesmo com a atribuição tendo sido feita). Isso é esperado pela arquitetura de filas do Laravel (jobs são fire-and-forget; o retorno não faz parte do contrato), mas quebrava silenciosamente a contagem mostrada ao usuário ("0 lead(s) distribuído(s)." mesmo distribuindo leads de fato). **Lição registrada**: se um Job precisa retornar um valor para quem o disparou (não só rodar em background), ou ele não deve implementar `ShouldQueue`, ou o resultado deve ser persistido em algum lugar legível depois (mesmo padrão já usado por `ProcessCsvImportJob`, que nunca lê retorno de `dispatchSync()` e em vez disso releva o resultado no próprio model `ImportBatch`).
7. ✅ **Concluída.** Lead Scoring básico: `LeadScoringService::calculateFitScore()` soma os `score_weight` de `lead_scoring_rules` ativas cujo `criterion`/`criterion_value` casa com a Company do lead — critérios suportados: `company_size` (igualdade direta com o enum da Company), `tax_regime` (idem), `state` (via `company.state.uf`), `cnae` (via `company.primaryCnae.code`), `revenue_range` (`criterion_value` no formato `"min-max"`, lado vazio = sem limite naquela ponta, ex.: `"4800000-"` para "acima de"). `calculateIntentScore()` é fórmula fixa (não configurável, conforme o plano): até 5 interações reais nos últimos 30 dias (10 pts cada, máx. 50) — conta só `call`/`whatsapp`/`email`/`visit`/`note`/`follow_up`, exclui `system`/`stage_change` (logs administrativos, não sinal de interesse do comprador) —, +30 pts se houver proposta `active`, com decaimento linear de 2 pts/dia após 7 dias sem interação (`lead.last_interaction_at`); cada subscore é clampado em [0,100]. `total_score` = fit + intent **sem normalização** (pode passar de 100) — thresholds Hot ≥80/Warm 60-79/Cold <60 aplicados direto sobre essa soma, exatamente como o plano descreve (e como o plano já avisa, ainda não calibrados com dados reais). `recalculate()` é chamado em todo ponto que pode mudar um dos subscores: `CreateLeadAction` (fit inicial, senão o lead ficaria "Cold" até o próximo job diário), `RegisterInteractionAction`, `CreateProposalVersionAction`, `CompleteFollowUpAction` — chamada direta nas Actions, **sem** introduzir a camada `Events`/`Listeners` que a lista mestra de arquitetura do plano menciona (`LeadInteractionRecorded`→`RecalculateIntentScore`): a Fase 5 já tinha estabelecido o precedente de incrementar `contact_attempts_count` direto na Action sem Event, então manter consistência evitou uma abstração nova para um único call-site por Action. `RecalculateLeadScoresJob` (todos os leads não-arquivados, cobre o decaimento por inatividade que não é disparado por nenhum evento) + `RecalculateLeadScoresCommand` (`leads:recalculate-scores`, `Schedule::...->daily()`); **propositalmente não implementa `ShouldQueue`**, mesmo motivo do `DistributeUnassignedLeadsJob` da Fase 6. UI mínima conforme o plano: `Chip` Hot(vermelho)/Warm(laranja)/Cold(cinza) no `LeadCard.tsx` (`utils/leadScore.ts` com os thresholds) + uma linha "Score: fit X + intent Y = Z" na aba Dados do `LeadDetailDialog` (não pedida explicitamente, mas trivial e útil). Sem Admin UI para CRUD de `lead_scoring_rules` — mesmo precedente da Fase 6 com `lead_distribution_rules` (regras geridas via seeder/tinker no MVP; `LeadScoringRuleController` listado na arquitetura mestra do plano é para uma fase de Admin ainda não escopada). Validado com 107 testes Pest (16 novos: matching de cada critério, clamp em 100, cap/decaimento/floor do intent score, exclusão de `system`/`stage_change` da contagem, recálculo inicial na criação, job pulando leads arquivados, recálculo após registrar interação) + Pint + `npm run build` + verificação manual no navegador (3 leads com perfis Hot/Warm/Cold mostrando os badges certos e o breakdown de score no diálogo). **Bug real de biblioteca encontrado pelos testes (não só na verificação manual)**: `Carbon::now()->diffInDays($maisAntigo)` no Carbon 3 retorna valor **com sinal** por padrão (negativo quando o argumento é uma data passada), não mais absoluto como em versões anteriores — um teste que esperava decaimento aplicado falhava porque `$inactiveDays` vinha negativo e a condição `> 7` nunca era verdadeira. Corrigido passando `absolute: true` explicitamente. **Lição**: ao calcular "dias desde X" com Carbon 3, sempre passar `absolute: true` (ou inverter os operandos) em vez de assumir o comportamento de versões anteriores do Carbon.
8. ✅ **Concluída.** SLA + auditoria avançada: `EvaluateSlaForLeadAction` avalia os 4 tipos de `sla_alerts` do plano por lead — `no_interaction_24h` (`last_interaction_at ?? created_at` há mais de 24h), `negotiation_15d` (stage=negotiation, `stage_entered_at` há mais de 15 dias), `no_return_7d` (stage=proposal_sent, sem retorno há mais de 7 dias — interpretação adotada para "sem retorno", não explicitada literalmente no plano), `attempts_exhausted` (`contact_attempts_count >= 7`) — e mantém os alertas em sincronia com a condição atual: cria um novo só se não houver um `resolved_at IS NULL` do mesmo tipo (idempotência do plano), e **resolve automaticamente** quando a condição deixa de se aplicar (extensão própria, não explicitada no plano, mas necessária — sem isso alertas nunca desapareceriam mesmo após o consultor agir). `LeadStageTransitionService` resolve todos os alertas abertos de um lead ao atingir Won/Lost (são terminais, o job de avaliação nem olha mais para eles). `EvaluateLeadSlaJob`/`ArchiveStaleLeadsJob` seguem o mesmo padrão das Fases 6-7: **sem `ShouldQueue`**, pelo motivo já documentado (retorno de `dispatchSync()` é descartado em jobs de fila). `ArchiveStaleLeadsJob` (`leads:archive-stale`, mensal) usa um limiar fixo de 12 meses como constante de classe — mesmo precedente dos limiares de `ComplianceChecker` (2/dia, 15/mês), evitando resolver agora a decisão aberta "tabela `settings` vs `.env`" para parâmetros configuráveis. Notificação ao gestor: `SlaAlertRaised` (`Illuminate\Notifications\Notification`, canais `mail`+`database`) enviada a todos os usuários `admin`/`manager` a cada alerta novo criado — **síncrona, não enfileirada** (mesma lógica de não usar `ShouldQueue`: sem worker no Sail, uma notificação enfileirada nunca apareceria no canal `database`, que é o mecanismo real de "notificação in-app" do plano). O envio é protegido por `try/catch` só ao redor da notificação (não do alerta em si) — um SMTP mal configurado em dev/staging (`.env` tinha host/credenciais vazios) não pode impedir a criação do `sla_alert`, que é o dado que importa; mudei `MAIL_MAILER` para `log` no `.env` local (gitignored, não afeta produção) para poder verificar o fluxo completo no navegador. Bell de notificações no `MuiAuthenticatedLayout.tsx` (visível só a admin/manager, badge com contagem via `unreadNotifications` compartilhado em todo request por `HandleInertiaRequests::share()`) + `NotificationController::markAsRead()` (`POST /notifications/{id}/read`, autorização por `notifiable_id`). `AuditController` (novo) usa o `AuditLogReconstructor`/`AuditLogPolicy` que já existiam desde a Fase 1 sem nunca terem sido expostos por uma rota — `index()` é o feed global filtrável de `audit_logs` (entidade/origem/período) e `show()` é a linha do tempo completa de uma entidade (`Lead`/`Company`/`Proposal`/`FollowUp`, mapeados por slug fixo na URL em vez do FQCN crú, para não aceitar string de classe arbitrária) com o seletor de data/hora que chama `stateAt()` e mostra o estado reconstruído ao lado do atual. Sem Admin UI para CRUD de `lead_distribution_rules`/`lead_scoring_rules` — este item também não, mantendo o mesmo precedente das Fases 6-7. Validado com 124 testes Pest (17 novos: cada tipo de alerta, idempotência, auto-resolução, terminal/arquivado pulado, notificação a admin/manager mas não consultor, resolução no Won/Lost, arquivamento com variações de referência temporal, RBAC + reconstrução de estado passado no AuditController) + Pint + `npm run build` + verificação manual no navegador (4 leads disparando os 4 tipos de alerta, bell mostrando os 6 alertas e decrementando ao marcar como lido, e-mails reais gravados em `storage/logs/laravel.log` via `MAIL_MAILER=log`, tela de auditoria listando/filtrando e reconstruindo o estado de um Lead). Nenhum bug de aplicação encontrado nesta fase — só a configuração de mail local, que não é bug, é infraestrutura de dev nunca antes exercitada.
9. ✅ **Concluída.** Painel gerencial básico: `DashboardController::index()` (rota `dashboard`, antes um Inertia estático) aplica as 2 fórmulas fechadas no plano (Taxa de Conversão = `won / (total - new)`, Ticket Médio = `sum(won_value) / count(won)`) sobre um recorte filtrável por período (`created_at`, padrão últimos 30 dias)/equipe/consultor/origem — escopo explícito do item do checklist, sem a segmentação por CNAE que a nota de "Fórmulas" das decisões de arquitetura também menciona (essa é guidance geral do sistema, não obrigação desta fase específica; CNAE fica para quando houver demanda real). Contagens por etapa/consultor/equipe/origem via `groupBy` + lookup de nomes (`countBy`/`countByRelated`). RBAC idêntico ao `LeadController::board()`: consultor só vê próprios leads/da equipe. **Gap real descoberto e corrigido nesta fase**: `leads.lead_source_id` existia desde a Fase 1 (coluna + relação + seeder `LeadSourcesSeeder` com Landing Page/Meta Ads/Google Ads/CSV/API/Manual) mas **nunca era preenchido em lugar nenhum do código** — toda contagem "por origem" seria sempre vazia. Corrigido em `CreateLeadAction::execute()` (novo parâmetro opcional `?int $leadSourceId`, default = origem "Manual" via lookup por nome) e em `ImportCompaniesFromCsvAction` (passa explicitamente a origem "CSV", resolvida uma vez e memoizada na própria instância da Action, não por linha do CSV). Export CSV (`GET /dashboard/export`) usa `response()->streamDownload()` com os mesmos filtros da tela, uma linha por Lead (company/CNPJ/etapa/consultor/equipe/origem/valor ganho/datas) — decisão própria de exportar o nível-linha (não o agregado), padrão mais útil/comum em CRMs para quem quer pivotear os dados depois. Dashboard não filtra `archived_at` — o filtro de período já decide o recorte, e leads arquivados são oportunidades reais que existiram naquele intervalo (excluí-los enviesaria a Taxa de Conversão histórica para baixo). Frontend: `Dashboard/Index.tsx` substitui o antigo placeholder estático — filtros (datas/equipe/consultor/origem) + 5 cards de resumo + 4 listas de breakdown com barra proporcional em CSS puro (sem nova lib de gráfico, nenhuma já estava no `package.json` e "básico" não justificava adicionar uma). Validado com 130 testes Pest (6 novos: fórmulas com dataset conhecido, cada filtro isoladamente, RBAC de consultor, `lead_source_id` populado em criação manual e via CSV, conteúdo do CSV exportado) + Pint + `npm run build` + verificação manual no navegador (8 leads de demonstração cobrindo as 8 etapas + 3 origens + 1 equipe, cards e breakdowns batendo exatamente com o cálculo manual, filtro de equipe abrindo as opções certas, export CSV confirmado via request autenticado real — `Content-Disposition: attachment` + linhas corretas).
10. ✅ **Concluída.** Hardening final: cada item literal do checklist já tinha cobertura de uma fase anterior, exceto 4 lacunas reais fechadas nesta fase. (a) **N+1 no board**: medido com `DB::enableQueryLog()` via tinker e depois travado em teste de regressão (`LeadManagementTest`) — `LeadController::board()` já eager-carrega `company`/`assignedTo`/`team`/`proposals.attachments`/`proposals.createdBy`/`followUps.createdBy`/`interactions.user` corretamente desde a Fase 4-6 (todas batched em `WHERE ... IN (...)`, nenhuma por-linha); o teste prova que o número de queries **não muda** entre 2 e 6 leads com proposta+follow-up+interação cada (13 queries nos dois casos) — nenhum bug encontrado, só lacuna de regressão coberta. Achado curioso no processo: a primeira versão do teste apontou uma "explosão" de 14→50 queries, que investigação revelou ser um bug do *teste* (o `DB::flushQueryLog()` estava posicionado antes da criação dos leads extras, então os `INSERT`s de setup también entravam na contagem) — corrigido movendo o flush para depois do setup; o board nunca teve o problema. (b) **Dedupe de CNPJ dentro do mesmo arquivo CSV** (não só entre duas importações separadas, que já era testado): nova linha duplicada no mesmo CSV upserta a mesma Company duas vezes e cria só 1 lead (`CsvImportTest`). (c) **Append-only**: `RbacPolicyTest` só cobria `LeadStageHistory`; estendido para `LeadInteraction` e para o próprio `AuditLog` (`update`/`delete` sempre `false`, inclusive para admin). (d) **RBAC de `CompanyPolicy::viewPartnerCpf`**: ability existia desde a Fase 1 sem nenhum teste — adicionado (admin/manager permitido, consultant negado); nota: a leitura de CPF de sócio ainda não tem controller/endpoint real (sócios ficaram fora do wizard de importação desde a Fase 2), então o "registro de auditoria a cada leitura" mencionado na seção de Verificação permanece não-implementável até que essa funcionalidade (fora do escopo do MVP atual) seja construída — documentado aqui em vez de inventar uma UI fora de escopo só para fechar a frase literal do checklist. Demais itens (transições inválidas, RBAC geral, validação de janela de horário, aviso de opt-out, diffs do `Auditable` em update/import, reconstrução de estado em data passada) já tinham teste sólido desde as Fases 3/5/8 — confirmados, não duplicados. Validado com 133 testes Pest (9 novos) + Pint + `npm run build`.

**MVP completo** — todas as 10 fases do plano original concluídas.

## Hardening pós-MVP (além do MVP original)

Trabalho adicional pedido pelo usuário após o MVP: análise de escalabilidade/robustez/consistência sobre a base já entregue, sem novas funcionalidades. Numerado H1–H4, separado do checklist 1–10 acima (que permanece intacto como o registro histórico do MVP original). Decisão de escopo: hardening puro, em fases pequenas e testáveis separadamente (mesmo padrão das 10 fases do MVP).

A auditoria inicial usou 2 agentes Explore (backend e frontend); vários achados deles **não se confirmaram** ao verificar manualmente contra o código/banco reais antes de aceitar qualquer um como item do plano — registrado aqui para não repetir a checagem: "índices faltantes em `assigned_to`/`team_id`/`lead_source_id`/`lead_stage_history.lead_id`/`company_partners.company_id`" é falso (toda coluna `foreignId()->constrained()` já tem índice implícito criado pela própria FK constraint do InnoDB — confirmado via `SHOW INDEX FROM leads`); "jobs carregam todos os leads em memória sem chunk" é falso (`Builder::each()` do Laravel já usa `chunk(1000)` internamente, confirmado no código-fonte do framework); "`ComplianceChecker` pode quebrar com `$lead->company` null" é falso (`leads.company_id` não é nullable e usa `restrictOnDelete()`, uma Company referenciada por Lead nunca pode ser deletada).

H1. ✅ **Concluída.** Robustez dos jobs agendados + índice de `archived_at`. **Bug real de corretude encontrado** (não documentado antes, distinto de qualquer achado de performance): `ArchiveStaleLeadsJob` e `DistributeUnassignedLeadsJob` filtravam por `whereNull('archived_at')`/`whereNull('assigned_to')` e usavam `->each()` (= `chunk()` por `OFFSET`), enquanto o próprio callback gravava exatamente essa coluna linha a linha — a partir do 2º lote, registros já processados saem do resultado e o `OFFSET` seguinte pula um número de leads igual ao tamanho do lote (confirmado isolando a lógica antiga via tinker com 105 leads: só 100 eram processados, 5 ficavam órfãos silenciosamente). Corrigido trocando `each()`/`chunk()` por `chunkById()` (pagina por `id > $ultimoId`, imune a esse efeito) nos dois jobs, com `private const CHUNK_SIZE = 100`. Nos 4 jobs agendados (`RecalculateLeadScoresJob`, `EvaluateLeadSlaJob`, `ArchiveStaleLeadsJob`, `DistributeUnassignedLeadsJob`) o processamento de cada lead passou a ser isolado em `try/catch (\Throwable)` com `Log::error()` — uma falha em um lead não aborta mais o restante do lote silenciosamente. Nova migration adiciona índice em `leads.archived_at` (única lacuna real de índice encontrada — filtrada em quase toda query do sistema, sem índice implícito por não ser FK). Validado com 139 testes Pest (6 novos: regressão do bug de chunk para os 2 jobs afetados, criando mais leads que `CHUNK_SIZE` e confirmando que **nenhum** fica sem processar; resiliência a falha de item único para os 4 jobs, confirmando que o lote continua e o erro é logado) + Pint.

H2. ✅ **Concluída.** Regras de negócio configuráveis (tabela `settings`). Nova tabela key-value (`key` único, `value` text em JSON para preservar tipo) + `App\Models\Setting` com `Setting::get(key, default)`/`Setting::set(key, value)`, cacheados via `Cache::rememberForever()` e invalidados em `set()` — sem ida ao banco em toda avaliação de compliance/SLA/scoring além da 1ª de cada chave. `SettingsSeeder` (chamado no `DatabaseSeeder`, primeiro da lista) popula as 9 chaves com os valores que já eram `const`/literal hardcoded antes desta fase — zero mudança de comportamento por padrão, confirmado pelos 139 testes anteriores continuando a passar sem alteração. Migradas: `ComplianceChecker` (`compliance.max_calls_per_day`=2, `compliance.max_calls_per_month`=15 — eram literais `2`/`15` inline, nem tinham nome de const), `EvaluateSlaForLeadAction` (`sla.no_interaction_hours`=24, `sla.negotiation_days`=15, `sla.no_return_days`=7, `sla.attempts_exhausted_threshold`=7), `ArchiveStaleLeadsJob` (`leads.archive_stale_months`=12), `LeadScoringService` (`scoring.intent_decay_grace_days`=7, `scoring.intent_decay_rate_per_day`=2 — só esses dois parâmetros do scoring entraram, não a janela de 30 dias/máx. 5 interações/pontos por interação, que o plano de hardening não havia escopado). **Sem Admin UI nesta fase** — mesmo precedente de `lead_distribution_rules`/`lead_scoring_rules` (editado via `Setting::set()` em tinker/seeder). Validado com 147 testes Pest (8 novos: `Setting::get/set` com default/valor salvo/atualização/preservação de tipo via JSON; 1 teste de comportamento por classe migrada confirmando que mudar o `Setting` muda o resultado — limite de chamada mais baixo dispara aviso antes, janela de SLA mais curta dispara alerta antes, limiar de arquivamento mais curto arquiva lead mais novo, decaimento de intent score com taxa/carência customizadas dá resultado diferente do default) + Pint + `migrate:fresh --seed` confirmando as 9 chaves populadas corretamente.

H3. ✅ **Concluída.** Paginação por coluna no Kanban. `LeadController::board()` continua fazendo a mesma query única já provada N+1-safe na Fase 10 (todas as leads não-arquivadas, com os mesmos eager loads) — a mudança é só na hora de montar `columns`: cada coluna agora expõe só os primeiros `PER_COLUMN_LIMIT` (30, `const` na classe) leads daquele estágio, ordenados por `stage_entered_at desc` (mais recentes primeiro), mais um `total` com a contagem real do estágio. Quantidade carregada por estágio é lida de `loaded[stage]` na query string (default `PER_COLUMN_LIMIT`, nunca menor que isso). **Decisão deliberada de escopo, diferente da redação original do plano**: a fatia por coluna acontece em memória (PHP `Collection::take()`), não via `LIMIT` no SQL — os dois custos reais citados no plano ("pesa no servidor (payload Inertia) e no navegador (render de N cards)") são resolvidos assim igual a fazer o corte no SQL, sem precisar de window function (`ROW_NUMBER() OVER PARTITION BY`) para aplicar limites *diferentes* por estágio numa única query — complexidade que essa base de código não usa em nenhum outro lugar. O trade-off é que o `WHERE` segue buscando todas as leads do banco (mesmo custo de antes, já provado barato) só não as renderiza/envia todas. Se o volume um dia justificar otimizar a própria busca no banco, é um próximo passo claramente isolado. Frontend: `Board.tsx` recebe `loaded` (novo prop) e `total` em cada coluna; botão "Carregar mais (N restantes)" aparece quando `total > leads.length` e chama `router.get(route('kanban.board'), { loaded: { ...loaded, [stage]: leads.length + 30 } }, { only: ['columns', 'loaded'], preserveState: true, preserveScroll: true })` — partial reload do Inertia (só essas 2 props recarregam), mesma convenção de nunca guardar o lead por objeto em `state` (Fase 3). Validado com 150 testes Pest (3 novos: cap no limite padrão + `total` correto, mais leads retornadas com `loaded` maior, ordenação por `stage_entered_at desc`) + Pint + `npm run build` + verificação manual no navegador (42 leads num estágio, coluna mostrando 30 + "Carregar mais (12 restantes)", clique confirmado via inspeção de rede — `GET /kanban?loaded[qualified]=60` → 200 — carregando os 42 e o botão desaparecendo; diálogo de detalhe continua abrindo normalmente nos leads carregados).

H4. ✅ **Concluída.** Consistência de frontend. `Dashboard/Index.tsx`: campos de data (`<input type="date">`) separam `onChange` (só atualiza estado local, `updateLocalFilter`) de `onBlur` (dispara `applyFilters()`, a visita Inertia de fato) — sem debounce/timer, simplesmente o filtro só "aplica" quando o usuário sai do campo; selects (equipe/consultor/origem) continuam em `onChange` direto, já disparavam 1 request por seleção, não por tecla. Confirmado via inspeção de rede: 0 requests durante o preenchimento do campo, 1 só no blur. Novo `resources/js/utils/format.ts` com `formatCurrency(value: number | string | null): string | null` e `formatDate(value: string | null): string`, consolidando as 6 implementações inline duplicadas de `toLocaleString('pt-BR', ...)` em `Dashboard/Index.tsx`, `Admin/Audit/Index.tsx`, `Admin/Audit/Show.tsx`, `ProposalsSection.tsx`, `InteractionsSection.tsx`, `FollowUpsSection.tsx` — unificou também o formato de moeda (a versão antiga em `ProposalsSection.tsx` concatenava `R$` manualmente; a nova usa `Intl` via `style: 'currency'`, mesmo padrão que o Dashboard já usava, sem diferença visual perceptível). Tipos de domínio (`ProposalAttachment`, `ProposalEntry`, `FollowUpEntry`, `InteractionType`, `LeadInteractionEntry`, `LeadCardData`) movidos de `Kanban/Partials/LeadCard.tsx` (que os declarava só por ter sido o primeiro arquivo a precisar deles) para `resources/js/types/index.d.ts`; os 5 arquivos que os importavam de lá (`Board.tsx`, `ProposalsSection.tsx`, `FollowUpsSection.tsx`, `InteractionsSection.tsx`, `LeadDetailDialog.tsx`) passaram a importar de `@/types` — 2 desses arquivos já tinham um import de `@/types` separado (`PageProps`) que foi unificado num só. Refactor puro, sem mudança de comportamento esperada — confirmado pelos 150 testes Pest (todos backend, nenhum tocado) continuando a passar + `npm run build` limpo + verificação manual no navegador (moeda e data formatadas igual a antes no diálogo de proposta, filtro de data do Dashboard só aplicando no blur).

**Hardening pós-MVP completo** — H1 a H4 concluídas.

## Redesign visual + tema customizável pelo admin (além do MVP original)

Pedido do usuário: reestruturar todo o visual para "mais bonito, tecnológico, moderno e funcional", com paleta de cores (Primary/Secondary) editável pelo admin. Direções confirmadas com o usuário antes de implementar: tema claro + dark mode com alternância (preferência pessoal por dispositivo, não definida pelo admin); admin customiza só Primary/Secondary, não a paleta inteira; edição numa página "Aparência" com preview ao vivo antes de salvar; a paleta vale para toda a aplicação autenticada **e também para login/registro**.

✅ **Concluído.** Não existia `ThemeProvider`/`createTheme()` em lugar nenhum — MUI rodava 100% no tema default. Infraestrutura nova: `resources/js/theme.ts` (`createAppTheme(mode, primary, secondary)`) centraliza paleta + ajustes visuais "flat/moderno" (`shape.borderRadius: 10`, `AppBar`/`Drawer` sem `boxShadow` pesado, botões com `disableElevation`). `resources/js/Contexts/ThemeContext.tsx` exporta `ColorModeContext` + `AppThemeProvider`, que lê `theme.primary`/`theme.secondary` de `usePage().props.theme` — compartilhado em **toda** requisição Inertia via `HandleInertiaRequests::share()` (fora do bloco condicionado a usuário autenticado, por isso login/registro também refletem a paleta) — e guarda o modo claro/escuro em `localStorage` (não no banco: é conforto de leitura pessoal, não identidade de marca). `resources/js/app.tsx` envolve cada página resolvida com `AppThemeProvider` dentro do `resolve()` do `createInertiaApp` — **descoberta**: não dá para mutar `page.default` diretamente (`Cannot assign to property 'default' of [object Module]`), porque módulos ES dinâmicos vêm congelados em build de produção (funcionava em dev, quebrava só em `npm run build`); a correção foi sempre retornar um objeto novo `{ default: ComponenteEnvolto }` em vez de mutar o módulo resolvido.

Persistência reaproveita a tabela `settings` da H2 (chaves `theme.primary_color`=`#4F46E5`, `theme.secondary_color`=`#06B6D4`, sem migração nova). Nova tela `Admin/Appearance/Index.tsx` (`GET`/`PUT /admin/appearance`) — **admin apenas**, não manager (`Gate::define('manage-appearance', fn ($user) => $user->role === UserRole::Admin)`, diferente de `admin.audit.*` que é admin+manager) — com preview ao vivo: os campos em edição alimentam uma segunda instância local de `createAppTheme` envolvendo amostras de `Button`/`Card`/`Chip`, sem afetar o resto da página até o usuário salvar. Sem lib de color picker nova — `<input type="color">` nativo + `TextField` de hex ao lado (`ColorPickerField`, novo componente pequeno).

Páginas que ainda eram 100% Tailwind/Breeze (`Auth/{Login,Register,ForgotPassword,ResetPassword,ConfirmPassword,VerifyEmail}.tsx`, `GuestLayout.tsx`, `Profile/Edit.tsx` + 3 Partials) foram migradas para MUI — sem isso não herdariam a paleta customizada, já que usavam classes/cores Tailwind hardcoded (cinza/indigo) via os componentes do Breeze. Depois da migração, `resources/js/Components/*` (Breeze: `PrimaryButton`, `TextInput`, `InputLabel`, `Checkbox`, `Modal`, `Dropdown`, etc.) e `Layouts/AuthenticatedLayout.tsx` ficaram sem nenhum import — removidos; `@headlessui/react` (só usado por `Modal`/`Dropdown`) saiu do `package.json`. `Welcome.tsx` (rota `/`) ficou deliberadamente fora desta fase — era a landing padrão do Breeze (screenshot da doc do Laravel), não uma tela real do fluxo do CRM; removida depois (ver seção "Login na raiz" mais abaixo), quando `/` passou a mostrar a tela de login.

Dois achados durante a verificação manual no navegador, ambos corrigidos antes de considerar a fase concluída:
1. Cores fixas como `bgcolor: 'grey.50'`/`'grey.100'` (usadas nos headers de coluna do `Kanban/Board.tsx` e na barra de progresso do `Dashboard/Index.tsx`) ignoram `palette.mode` — vira baixo contraste no dark mode (texto claro do tema sobre fundo claro fixo). Trocadas por tokens mode-aware (`action.hover`, `background.paper`, `divider`).
2. O teste de regressão de N+1 do board (`LeadManagementTest`) passou a falhar depois de `theme` entrar no `share()`: a 1ª requisição da medição populava o cache do `Setting` (cache miss = 2 queries extra de `SELECT`), a 2ª lia do cache (hit = 0 queries) — simulando uma "diferença" de contagem que não vinha do código sob teste. Resolvido com uma requisição de aquecimento de cache antes de ligar `DB::enableQueryLog()` — mesma família do lesson já registrado sobre posicionamento de `flushQueryLog()` (Fase 10/H1).

Validado com 155 testes Pest (5 novos em `AppearanceControllerTest`: admin acessa e atualiza, manager/consultant recebem 403, hex inválido retorna 422, `theme` compartilhado reflete o valor atual mesmo em rota de guest) + Pint + `npm run build` + verificação manual completa no navegador (login/registro com a paleta aplicada, troca de Primary/Secondary refletindo no Kanban/Dashboard imediatamente após salvar sem novo login, dark mode persistindo após reload, RBAC confirmado via requisição direta a `/admin/appearance` como manager).

## Enriquecimento via Google Places API + busca avançada de Companies (além do MVP original)

Pedido do usuário a partir de uma documentação informal própria propondo integração com "Google Meu Negócio" no card do lead, uma página de busca avançada, selos automáticos e um "score automático" baseado em sinais do Google. Antes de implementar, análise técnica corrigiu a premissa central e reconciliou os outros itens contra funcionalidades que já existiam:

1. **A API certa é a Google Places API (New), não a Business Profile API** ("Google Meu Negócio") — esta última só dá acesso a perfis que a própria empresa verificou e autorizou; não existe caminho de um CRM consultar o perfil do Google de uma empresa-prospect arbitrária por ela. A Places API é pública, mas paga por requisição (SKUs por faixa de campo — `rating`/`reviews` caem no SKU mais caro) e devolve no máximo ~5 reviews por local.
2. "Score automático do lead" já existia desde a Fase 7 (`LeadScoringService`, `fit_score`/`intent_score`/`total_score`) — decisão foi estender `ruleMatches()` com novos critérios, não criar um motor paralelo.
3. Uma página nova "Pesquisa de Leads" duplicaria `Companies/Index.tsx` (já tinha busca textual + paginação + dialog de detalhe desde a Fase 2) — decisão foi estender essa tela.
4. "Possui WhatsApp"/"possui redes sociais" não são campos da Places API — saem do enriquecimento automático, continuam dado manual em `company_contacts` (existia desde a Fase 1), sem selo automático associado.

✅ **Concluído.** Esta é a **primeira integração HTTP externa do projeto** (nenhuma chamada `Http::` existia antes) — `app/DomainServices/GooglePlacesClient.php` usa `Illuminate\Support\Facades\Http` com timeout de 5s + `try/catch` retornando `null` em qualquer falha (nunca lança exceção), mesmo princípio não-bloqueante já usado pelo `ComplianceChecker`/`SlaAlertRaised`: uma falha do lado do Google nunca pode quebrar a tela do lead. Credencial em `config/services.php` (`'google_places' => ['key' => env('GOOGLE_PLACES_API_KEY')]`, mesmo padrão de Postmark/Resend/SES/Slack já no arquivo) — deliberadamente **fora** da tabela `settings`: `Setting` é para configuração de negócio, não para secrets/credenciais.

Nova tabela satélite `company_places_profiles` (FK `company_id` única, `restrictOnDelete()`, mesmo padrão de `company_financial_snapshots`) + `Company::placesProfile(): HasOne`. `app/Actions/Companies/SyncGooglePlacesProfileAction.php` orquestra a sincronização sob orçamento controlado: sem `GOOGLE_PLACES_API_KEY` configurada → resultado `not_configured` (no-op, nada persistido); orçamento mensal esgotado (`Setting` `google_places.monthly_budget_cap`, default 500) → `budget_exceeded` — o contador (`google_places.budget_month`/`google_places.budget_used`) reaproveita a tabela `settings` da H2 em vez de uma tabela nova, com reset automático na virada do mês; sem `google_place_id` salvo ainda, resolve via Text Search por nome+endereço antes de buscar os Place Details (com `FieldMask` explícito, nunca o corpo todo da resposta). Controller (`GooglePlacesProfileController::store`) sem Gate extra — mesma visibilidade aberta de `CompanyPolicy::viewAny()` (já retornava `true` para todos os papéis antes desta feature).

Frontend: botão "Sincronizar"/"Atualizar" + dados do perfil (nota, nº de avaliações, categoria, status operacional) num card novo (`GooglePlacesCard.tsx`) dentro da aba "Dados" do `LeadDetailDialog.tsx` — decisão de não duplicar em `CompanyDetailDialog.tsx` também: o consultor vive no Kanban, é o mesmo lugar onde o breakdown de `fit_score`/`intent_score` já aparece desde a Fase 7. Selos automáticos (Sem site / Poucas avaliações / Nota baixa / Perfil incompleto) computados no componente contra thresholds (`google_places.low_rating_threshold`=3.5, `google_places.low_review_threshold`=10) passados via prop `googlePlacesBadgeThresholds` desde `LeadController::board()`, em vez de hardcoded duas vezes (backend e frontend).

Scoring: `LeadScoringService::ruleMatches()` ganhou 3 critérios novos (`has_google_profile`, `google_rating_above`, `google_review_count_above`) no mesmo `match` que já tratava `company_size`/`tax_regime`/`state`/`cnae`/`revenue_range` — sem motor novo, sem Admin UI (mesmo precedente de `lead_distribution_rules`/`lead_scoring_rules`, regras geridas via seeder/tinker). `LeadScoringRulesSeeder` ganhou 2 linhas de exemplo.

Busca avançada: `CompanyController::index()`/`Companies/Index.tsx` ganharam filtros de estado (`state_id`), cidade (busca por nome, não dropdown dependente — evita um endpoint novo só para listar cidades por estado), responsável (`assigned_to` do lead), status (`stage` do `LeadStage` real, não uma lista simplificada nova) e período de criação do lead (`created_from`/`created_to`) — todos opcionais e combináveis com `search`/`without_active_lead` já existentes via `whereHas('leads', ...)` quando o filtro é atributo do Lead, ou `where()` direto quando é atributo da Company.

Validado com 165 testes Pest (10 novos: `GooglePlacesProfileTest` — sincronização bem-sucedida com `Http::fake()`, `not_configured` sem chamar `Http::` nenhuma vez, orçamento mensal bloqueando a 2ª chamada, critérios novos de scoring mudando o `fit_score`, rota acessível por qualquer papel autenticado; `CompanySearchFiltersTest` — cada filtro novo isoladamente e combinado com a busca textual existente) + Pint + `npm run build` + verificação via Tinker contra o banco de desenvolvimento real (sem `GOOGLE_PLACES_API_KEY` configurada localmente — `/kanban` e `/companies` renderizam normalmente, sincronização retorna `not_configured` sem erro).

## Admin CRUD + Produto↔Proposta + ações em massa + gráficos/digest de SLA (além do MVP original)

Pedido do usuário: mapear relacionamentos/lacunas do sistema já construído e propor novas funcionalidades que enriquecessem o fluxo de trabalho. O mapeamento (todos os models/rotas/controllers) achou várias lacunas reais; o usuário escolheu 4 das opções levantadas para implementar. Uma quinta opção considerada ("rastrear qual `lead_distribution_rule` atribuiu um lead") **já existia** desde a Fase 6 (`lead_assignments.lead_distribution_rule_id`, `LeadAssignment::distributionRule()`, populado por `LeadDistributionService::createAssignment()`) — só não estava exposta em nenhuma tela; resolvido expondo a contagem de atribuições por regra em `Admin/Rules/Index.tsx`, sem nenhum código de domínio novo.

✅ **Concluído.**

1. **Admin CRUD para `User`/`Team`/`Product`/`LeadDistributionRule`/`LeadScoringRule`** — todos só eram geridos via Tinker/seeder antes disso (precedente citado repetidamente nas Fases 6-7 e na H2). `User`/`Team`/`Product` ganharam `SoftDeletes` (mesmo trait já usado por `Company` desde a Fase 1) em vez de hard delete — todos têm FKs históricas `nullOnDelete` apontando para eles (`leads.assigned_to`, `proposals.created_by`, `lead_distribution_rules.product_id`); um delete de verdade colocaria `null` em registros históricos (perde "quem criou essa proposta", "qual produto foi vendido ali"). Confirmado por teste: um usuário soft-deleted não consegue mais autenticar (`Auth::attempt()` retorna `false`) — mas `$model->fresh()` **não** respeita o escopo global de `SoftDeletes` (usa `newQueryWithoutScopes()` internamente), então ainda encontra a linha com `deleted_at` preenchido; só a query padrão (`User::find()`, usada em todo o resto do app) fica cega para ela. Isso quebrou a asserção antiga em `ProfileTest` (`assertNull($user->fresh())` depois de auto-exclusão) — corrigida para refletir o comportamento novo, intencional. `LeadDistributionRule`/`LeadScoringRule` não ganharam `SoftDeletes`: já tinham `is_active`, então remover ali é só desativar via update — sem `destroy()` nenhum (a primeira tem um motivo a mais: hard delete apagaria a trilha de qual regra atribuiu cada lead historicamente). RBAC via Policies novas (convenção `Model`→`ModelPolicy`, auto-descoberta do Laravel): `UserPolicy`/`TeamPolicy` admin-only (`UserPolicy::delete()` bloqueia auto-exclusão); `ProductPolicy` admin+manager (catálogo comercial, menor risco); `LeadDistributionRulePolicy`/`LeadScoringRulePolicy` admin-only (mudar essas regras afeta todo o funil). 4 páginas novas (`Admin/Users`, `Admin/Teams`, `Admin/Products`, `Admin/Rules` com `Tabs` Distribuição/Scoring, mesmo padrão do `LeadDetailDialog`) — tabela + Dialog de criar/editar, sem página de edição separada, mesma convenção de `Companies/Index.tsx`/`Admin/Appearance/Index.tsx`.

2. **`Proposal.product_id`** — antes só `leads.won_product_id` existia (preenchido só ao ganhar o lead); a proposta em si não dizia qual produto estava sendo negociado. Migration aditiva (`product_id` nullable, `nullOnDelete`), `Proposal::product()`, `CreateProposalVersionAction`/`StoreProposalRequest` passam o campo adiante, `ProposalsSection.tsx` ganhou um `<Select>` reaproveitando o mesmo `products` que `LeadController::board()` já carregava para o `StageTransitionDialog` (só precisou repassar a prop por `LeadDetailDialog`).

3. **Ações em massa no Kanban** (`POST /leads/bulk-assignment`, `POST /leads/bulk-stage`) — sem lógica de negócio nova: reaproveitam `AssignLeadAction`/`LeadStageTransitionService` num loop com `try/catch` por item (mesmo princípio da H1: 1 falha isolada não aborta o lote, só entra numa contagem de sucesso/falha retornada via flash). `BulkUpdateLeadStageRequest` rejeita `Won`/`Lost` já na validação — motivo de perda/valor ganho são por lead, não fazem sentido aplicados a um lote (corte de escopo deliberado). RBAC é avaliado por lead dentro do loop (`Gate::authorize('assign'|'update', $lead)`), não há checagem global antes do loop — um consultor pode chamar o endpoint, simplesmente não tem nenhum lead aprovado se não tiver permissão sobre nenhum dos selecionados. Frontend: `Board.tsx` ganhou "modo de seleção" (toggle) — liga `disabled: selectable` no `useDraggable` do `@dnd-kit/core` de cada `LeadCard` (não bastava deixar de espalhar `listeners`/`attributes`; o hook continuava registrando o gesto de arrastar) e troca o clique-para-abrir-dialog por um `Checkbox`; uma barra de ação fixa aparece com ≥1 lead selecionado.

4. **Dashboard com gráficos (`recharts`) + digest de notificações de SLA** — `recharts` é a primeira lib de gráfico do projeto (`Dashboard/Index.tsx` usava barras 100% CSS desde a Fase 9). `DashboardController` não mudou: já devolvia `byStage`/`byConsultant`/`byTeam`/`byLeadSource` como `{label, total}[]`, formato que o `BarChart` consome direto. Cores do gráfico lidas via `useTheme()` (`palette.primary.main`/`divider`/`text.secondary`) — herdam a paleta customizável do redesign visual automaticamente, sem hardcode de cor nova. `SlaAlertRaised::via()` perdeu o canal `mail` (ficou só `database`, o sino in-app em tempo real) — o e-mail por alerta individual virou `SlaDigest` (notification nova), 1 e-mail/dia via `leads:send-sla-digest`/`SendSlaDigestJob` (mesmo padrão dos demais jobs agendados: sem `ShouldQueue`, despachado via `Schedule::command(...)->dailyAt('08:00')`), resumindo todos os `sla_alerts` não resolvidos agrupados por tipo — não envia nada se não houver alerta em aberto (sem digest vazio).

Validado com 192 testes Pest (27 novos: `Admin/{UserManagementTest,TeamManagementTest,ProductManagementTest,RuleManagementTest}` — CRUD + RBAC + soft delete bloqueando login; `Domain/ProposalProductTest` — proposta com/sem produto, rejeição de produto inexistente; `Kanban/BulkActionsTest` — sucesso, RBAC, falha parcial não abortando o lote, rejeição de etapa terminal; `Domain/SlaDigestTest` — agrupamento, digest vazio não enviado, `SlaAlertRaised` só com canal `database`) + Pint + `npm run build` + verificação via Tinker contra o banco de desenvolvimento real (todas as páginas Admin novas renderizando, `SendSlaDigestJob` processando os alertas em aberto reais sem erro).

## Login na raiz (além do MVP original)

Pedido do usuário: "tela de login original, para ser acessada na raiz, como o convencional". `GET /` renderizava `Welcome.tsx` (landing vestigial do Breeze, citada no Redesign visual, sem uso real desde a Fase 1 — usuários sempre entravam direto por `/login`).

✅ **Concluído.** `routes/web.php`: a rota `/` passou a apontar para `AuthenticatedSessionController::create()` (o mesmo Controller de `GET /login`, sem Controller/view novo) com o middleware `guest` — literalmente a mesma tela `Auth/Login`. `RedirectIfAuthenticated` (middleware `guest` nativo do Laravel, já usado em `routes/auth.php`) resolve sozinho o "convencional" pedido: detecta a rota `dashboard` registrada e redireciona quem já está autenticado para lá automaticamente, sem precisar de lógica própria nessa rota. `Welcome.tsx` removido do repositório (confirmado sem nenhuma outra referência antes de apagar). `/login` continua existindo como rota nomeada separada (`routes/auth.php`) — ambas renderizam a mesma tela; é o padrão comum em apps sem landing pública.

Validado com a suíte completa (sem teste novo — comportamento cai inteiramente em middleware/Controller já testados em `tests/Feature/Auth/AuthenticationTest.php`) + `npm run build` + verificação manual via `curl` (guest na raiz recebe `component: "Auth/Login"`; usuário autenticado recebe redirect 302 para `/dashboard`).

## Refinamento visual do Kanban + tradução de termos + correção do logout (além do MVP original)

Pedido do usuário: deixar o Kanban "mais bonito, colorido, com contrastes e moderno", com cards "mais elegantes, com mais informações importantes que agilizam o processo". Segunda passada sobre o redesign visual já existente (tema/paleta), desta vez focada no Kanban em si.

✅ **Concluído.** `resources/js/theme.ts` ganhou gradientes, sombras customizadas e scrollbar estilizada. `Admin/Appearance/Index.tsx` ganhou um seletor de cor por estágio do Kanban (`stage_colors`, persistido em `Setting` ao lado de `theme.primary_color`/`theme.secondary_color`, compartilhado via `theme.stageColors` no `HandleInertiaRequests::share()`) — `Kanban/Board.tsx` usa essa cor na borda superior e no `Chip` de contagem de cada coluna. `LeadCard.tsx` ficou bem mais denso: borda lateral na cor do estágio, ícones de ligar/WhatsApp com `stopPropagation` (não abrem o `LeadDetailDialog` ao clicar), chip de valor quando há proposta `active`, chip do próximo follow-up (vermelho se atrasado), mini-avatar do consultor responsável e indicador "há N dias"/"sem contato" desde a última interação (`utils/format.ts` ganhou `formatShortDate`/`formatRelativeDays`).

**Bug real encontrado e corrigido, fora do escopo direto do pedido**: o botão de sair no `MuiAuthenticatedLayout.tsx` não disparava nenhuma ação. Causa: `<MenuItem component={Link} href={route('logout')} method="post" as="button">` deixava props específicas do MUI (`component`/`classes`) vazarem como atributos literais no elemento DOM final, e nenhum listener de clique chegava a ser registrado — confirmado via Playwright real (`page.waitForResponse` nunca disparava). Corrigido trocando por `onClick={() => router.post(route('logout'))}`, mesmo padrão já usado em outros pontos do mesmo arquivo (ex.: marcar notificação como lida).

Pedido explícito do usuário, válido para qualquer texto novo daqui em diante (não é tarefa "concluída e esquecida"): evitar palavras em inglês pouco usuais na UI em português, mantendo as já consagradas (login/logout/status/e-mail/Kanban/CSV/WhatsApp/Google), mas traduzindo jargão como "Scoring"→"Pontuação", "Round robin"→"Rodízio global", "Hot/Warm/Cold"→"Quente/Morno/Frio", "Admin"→"Administrador" — aplicado em `Admin/Appearance/Index.tsx`, `Admin/Rules/Index.tsx`, `Admin/Users/Index.tsx`, `utils/leadScore.ts`, `InteractionsSection.tsx` e títulos de página.

`LeadDetailDialog.tsx` (único dialog com abas) e o dialog de regra de distribuição em `Admin/Rules/Index.tsx` (muitos campos) alargados para `maxWidth="md"` com `Grid` de 2 colunas, a pedido do usuário ("dialogs mais largos onde têm muitos dados e abas") — dialogs simples de 1-3 campos não foram alterados, deliberadamente.

Validado com a suíte completa (sem regressão; nenhum teste novo específico, mudança é majoritariamente visual) + Pint + `npm run build` + verificação manual completa no navegador via Playwright, incluindo a confirmação do logout via response real (`302 /logout`).

## Configurações (admin) + Endereço normalizado + Mapa do lead (além do MVP original)

Três pedidos do usuário em sequência, implementados juntos por dependerem um do outro: (1) uma página de configurações para chaves de API e parâmetros de negócio, restrita a admin; (2) leads precisam de endereço, com um botão que busque a localização (latitude/longitude) automaticamente quando o endereço estiver completo; (3) pedido intermediário explícito, **decisão fechada com o usuário, não escolha livre**: "crie uma tabela apenas para armazenar os endereços e a relacione" — o endereço deixa de ser um grupo de colunas em `companies`.

✅ **Concluído.**

1. **`Admin/Settings/Index.tsx`** (`GET`/`PUT /admin/settings`, `Gate::define('manage-settings', ...)`, admin apenas — gate próprio, diferente de `manage-appearance`) reúne os parâmetros que viviam só em `Setting`/`.env` sem nenhuma UI desde a H2: limites de compliance (chamadas/dia e/mês), as 4 janelas de SLA, limiar de arquivamento, carência/taxa de decaimento do intent score, orçamento mensal e os 2 thresholds de selo do Google Places — mais a própria chave da Google Places API, com um Accordion de passo a passo de como gerá-la no Google Cloud Console. A chave segue `Setting::get('integrations.google_places_api_key') ?: config('services.google_places.key')` (banco tem prioridade sobre `.env`, mas `.env` continua funcionando em deploys que não tocarem a tela nova) e nunca é devolvida em texto puro — só `{configured: bool, masked: '••••'+4 últimos dígitos}`, mesma UX de "deixe em branco para manter a senha atual" já usada em `Admin/Users`.

2. **Endereço extraído para uma tabela satélite própria, `addresses`** (1:1 via `company_id` único, `restrictOnDelete()`, mesmo padrão de `company_places_profiles`) — migração nova (`2025_01_01_000450_create_addresses_table.php`) cria a tabela, faz backfill via `INSERT...SELECT` dos dados já existentes em `companies` e só então remove as 8 colunas de lá; `down()` faz o caminho inverso (testado nos dois sentidos contra o banco de dev real com 12 companies já cadastradas, sem perda de dado). Todo consumidor direto de `companies.city_id`/`state_id` precisou migrar para `company.address.city_id`/`state_id` — não é só um detalhe de exibição, são 2 regras de negócio reais: `LeadDistributionService::poolByGeography()` (estratégias `by_state`/`by_region`) e `LeadScoringService::ruleMatches()` (critério `state`). Também migrados: `ImportCompaniesFromCsvAction` (novo método privado `upsertAddress()`, separado de `buildCompanyAttributes()` — resolve `state`/`city` uma vez e grava na Company e no Address em 2 passos), `CompanyController::index()` (filtros `state_id`/`city` + eager-load), `CompanySeeder` e o helper `DemoDataSeeder::makeCompany()` (isola as chaves de endereço dos atributos da Company antes de criar as duas linhas, evitando editar os ~12 pontos de chamada individualmente). `Address` usa a trait `Auditable` como qualquer outro model auditável e foi adicionada ao `AuditController::ENTITY_SLUGS` (slug `address`) — aparece na tela de auditoria sem nenhum código novo de domínio, só a entrada no mapa de slugs.

3. **Nova aba "Mapa" no `LeadDetailDialog.tsx`** (`MapSection.tsx`, `react-leaflet@^4.2.1` + tiles do OpenStreetMap — **não** a versão `^5`, que exige React 19 e o projeto está em React 18.2/18.3) mostra a localização da Company quando há `latitude`/`longitude` sincronizadas (`company_places_profiles`, que ganhou essas 2 colunas numa migração aditiva), com um formulário de edição do endereço completo (logradouro/número/complemento/bairro/CEP + Estado/Cidade em cascata via novo endpoint leve `GET /lookups/cities?state_id=` por um `LookupController` dedicado — evita carregar a tabela `cities` inteira na página) restrito a admin/manager (mesmo critério de `CompanyPolicy::update`, decisão do usuário de manter o mesmo RBAC já existente para edição de Company em vez de abrir para consultor). Consultor vê o endereço em modo leitura, mas o botão "Buscar localização via Google" continua aberto a todos os papéis (comportamento que já existia desde a integração com Places, não restringido aqui). A busca reaproveita a sincronização da Places API já existente desde a fase de enriquecimento — sem integração nova: `GooglePlacesClient::DETAILS_FIELD_MASK` ganhou `location`, e `SyncGooglePlacesProfileAction::buildSearchQuery()` agora monta a Text Search com o endereço completo (rua, número, bairro, cidade, UF) em vez de só nome+rua, que falhava sempre que havia mais de uma empresa na mesma rua. Continua sem nenhuma chamada à Geocoding API — a Places API (New) permanece a única integração HTTP externa do projeto, decisão já fechada na fase de enriquecimento.

**Lição de UX, achada na verificação manual**: o `Dialog` do MUI centraliza verticalmente por padrão — como o conteúdo muda de altura entre as abas do `LeadDetailDialog` (e entre os formulários condicionais do `StageTransitionDialog`), o diálogo "saltava" de posição a cada clique de aba, dificultando a leitura. Resolvido fixando o topo via `slotProps={{ container: { sx: { alignItems: 'flex-start' } }, paper: { sx: { mt: 6, mb: 6 } } }}`, extraído para `resources/js/utils/dialog.ts` (`topAlignedDialogSlotProps`) e aplicado nos dois dialogs do Kanban.

Validado com 209 testes Pest (16 novos: `CompanyAddressTest` — CRUD do endereço, RBAC, validação de `city_id`/`state_id` inexistentes, lookup de cidades por estado; `GooglePlacesProfileTest` ganhou 2 testes novos para a busca com endereço completo vs. sem endereço algum; `SettingsControllerTest` — RBAC, persistência, mascaramento da chave) + Pint + `npm run build` + verificação manual completa no navegador via Playwright (edição de endereço com Estado/Cidade em cascata persistindo corretamente, mapa renderizando com Leaflet, dialogs fixos no topo em todas as abas, página de Configurações com os 12 parâmetros + chave mascarada).

## Máscaras de CNPJ/telefone/CEP (além do MVP original)

Pedido do usuário: "crie máscaras em todos os `TextField` e `Typography`... telefones, celular, CPF, CNPJ".

✅ **Concluído.** `resources/js/utils/format.ts` ganhou `formatCnpj`/`formatPhone`/`formatCep`/`formatCpf` (formatação para exibição) + `unmask` (volta só os dígitos, usado no `onChange` dos campos editáveis) — aplicadas em todo campo real de CNPJ/telefone/WhatsApp/CEP já existente na UI: `Companies/Index.tsx` (tabela), `CompanyDetailDialog.tsx` (CNPJ, lista de contatos, formulário de criar lead), `LeadDetailDialog.tsx` (telefone/WhatsApp do lead), `LeadCard.tsx` (tooltip "Ligar para..."), `InteractionsSection.tsx` (número discado, input e histórico) e `MapSection.tsx` (CEP do endereço). A máscara é puramente cosmética: o valor exibido no `TextField` é formatado, mas o estado do formulário (e o que é enviado ao backend) sempre guarda só dígitos — mesmo formato que o backend já assumia em todo lugar (`ComplianceChecker`/`phone_dialed`, link `wa.me`), então não houve nenhuma mudança de validação/contrato no lado do servidor. `formatCpf` não tem nenhum consumidor ainda — não existe tela que mostre CPF de sócio no projeto (`CompanyPolicy::viewPartnerCpf` existe desde a Fase 1 sem nenhum controller/endpoint real); criada já agora só para estar pronta quando essa funcionalidade futura existir, sem repetir a lição "auditoria a cada leitura de CPF continua não-implementável" registrada na Fase 10.

Validado com a suíte completa (sem teste novo — mudança é só de apresentação no frontend, nenhuma regra de validação/persistência mudou) + `npm run build` + verificação manual no navegador via Playwright (CNPJ formatado na listagem, telefone/WhatsApp formatados em tempo real durante a digitação no `LeadDetailDialog`).

## Reciclagem de lead perdido (além do MVP original)

Lacuna real do plano original, identificada numa análise pedida pelo usuário ("analise e planeje as próximas atualizações") antes de qualquer pedido de feature nova: `RecycleLeadAction` estava prevista desde a primeira versão deste documento (seção "Decisões de arquitetura para as lacunas da spec original", item 3) e a tabela `loss_reason_recycle_rules` é seedada desde a Fase 1 (`LossReasonRecycleRulesSeeder`, registrado em `DatabaseSeeder`) — mas nenhum código do projeto a lia antes desta fase. A tela "Companies sem lead ativo" (`Companies/Index.tsx`) já mostrava as candidatas desde a Fase 2, sem nenhuma ação disponível ali.

✅ **Concluído.** `app/Actions/Leads/RecycleLeadAction.php::execute(Lead $lostLead)` recusa com `DomainException` se o lead não estiver em `Lost`, e reaproveita `CreateLeadAction` (que já recusa criar um lead se a Company tiver um lead em andamento — mesma regra de "nunca duplicar lead aberto" da importação CSV) para criar o novo lead em `New`, marcando `is_recycled=true`/`recycled_from_lead_id` na sequência — tudo dentro de `DB::transaction`. O lead perdido original nunca é alterado (`recycledFrom()`/`recycledTo()` já existiam no model `Lead` desde a Fase 1, sem nenhum consumidor até agora). RBAC: `LeadPolicy::recycle()` é admin/manager — decisão do usuário, tratando reciclagem como decisão gerencial (mesmo critério já usado por `assign()`), não uma operação livre do consultor. Rota nova `POST /leads/{lead}/recycle`.

Frontend: `CompanyDetailDialog.tsx` mostra um aviso **não-bloqueante** (decisão do usuário, mesmo princípio de todo aviso de compliance do projeto desde a Fase 5: avisar, nunca bloquear) com a janela sugerida de reciclagem do motivo da última perda e quantos dias já se passaram desde então — dado vindo de `loss_reason_recycle_rules`, exposto via novo prop `lossReasonRecycleRules` de `CompanyController::index()`. O botão "Reciclar lead perdido" continua sempre clicável, mesmo antes da janela sugerida terminar ou quando o motivo normalmente não é recomendado para reciclagem (`is_recyclable=false`, ex.: "Sem interesse"/"Dados inválidos") — nesses casos o aviso só muda de cor (`warning` em vez de `info`). `CompanyController::index()` passou a eager-carregar `loss_reason`/`stage_entered_at` dos leads (antes só `id`/`stage`/`assigned_to`/`created_at`) para essa lógica funcionar inteiramente no frontend, sem round-trip extra ao escolher qual lead perdido reciclar.

Validado com 215 testes Pest (6 novos em `LeadRecycleTest`: reciclagem bem-sucedida criando o novo lead linkado corretamente, recusa para lead que não está em `Lost`, recusa quando a Company já tem lead aberto, RBAC de admin/manager vs. consultor, e o novo prop `lossReasonRecycleRules` exposto na página) + Pint + `npm run build` + verificação manual completa no navegador via Playwright contra o banco de dev real (empresa com lead perdido reciclada de verdade, confirmada via tinker: novo lead em `new`, `recycled_from_lead_id` apontando para o lead perdido original, e a empresa saindo do filtro "Sem lead ativo").

## Espaço para Fase 2 sem migração destrutiva

- `company_id` em toda tabela-chave → multi-tenant é middleware de scoping.
- `lead_interactions` já tem `duration_seconds`/`recording_url`/`outcome_code`/`phone_dialed`/`external_provider_ref`/`channel_metadata` → integrar CTI (Twilio/Asterisk) e WhatsApp Business API é popular esses campos via webhook, sem nova migração.
- `proposal_attachments.disk` abstrai storage → S3/MinIO é config.
- `audit_logs` genérico (actor polimórfico) já comporta novas entidades — basta aplicar a trait `Auditable` ao novo model.
- `lead_scoring_rules` já configurável → afinar pesos e tornar `intent_score` configurável é dado, não schema.
- Dashboard rico (AHT, FCR, ASA, CSAT, Occupancy) depende de dados de CTI que só existem após integração Fase 2 — schema já pronto para recebê-los.
- Gamificação: `consultant_performance_snapshots` (novo, aditivo) calculado por job periódico a partir de `lead_interactions`/`lead_stage_history`/`fit_score` já existentes — não exige nenhuma mudança retroativa.
- API pública de leads (Fase 2) reusa os mesmos `Actions/` por trás de um Controller API fino; dedupe por CNPJ já é o padrão usado na importação CSV.

## Riscos/decisões já fechadas com o usuário (registro)

- CPF de sócio armazenado completo, criptografado em repouso, acesso restrito e auditado.
- Avisos de compliance (janela de horário, empresa baixada, opt-out, frequência) sempre não-bloqueantes (avisar + exigir confirmação).
- Reimportação nunca recria lead automaticamente para company com lead Won/Lost — só sinaliza candidata a reciclagem.
- Importação de CSV precisa de mapeamento de colunas flexível com perfis reutilizáveis, não um parser fixo a um layout.
- Auditoria de Leads/Companies/Proposals/Follow-ups é só leitura/reconstrução de estado passado (sem ação de "restaurar"/"reverter"); visível apenas para admin/manager; origem (`actor_type`) distingue usuário, sistema/job agendado e importação CSV (com referência ao `import_batch_id`).

## Decisões ainda abertas para refinar durante a implementação (não bloqueiam o início)

- Fórmula exata de `total_score` (soma simples vs. ponderada) — calibrar com dados reais antes de fixar thresholds Hot/Warm/Cold.
- Escopo de estratégia `by_cnae` em `lead_distribution_rules` (dado já disponível; decidir se entra no MVP ou Fase 2 conforme tempo).
- Local de armazenamento dos parâmetros configuráveis (limiar de `contact_attempts_count`, janela de arquivamento) — tabela `settings` key-value vs. `.env`.

## Arquivos críticos

- `database/migrations/*` — todas as tabelas listadas (companies e satélites, leads, lead_interactions, opt_outs, import_*, scoring/recycle rules)
- `app/Enums/LeadStage.php`
- `app/DomainServices/LeadStageTransitionService.php`
- `app/DomainServices/CallWindowValidator.php`
- `app/Models/Lead.php`, `app/Models/Company.php`
- `app/Concerns/Auditable.php`
- `app/DomainServices/AuditContext.php`, `AuditLogReconstructor.php`
- `app/Http/Controllers/Kanban/LeadStageController.php`
- `app/Http/Controllers/Companies/ImportController.php`
- `app/Policies/LeadPolicy.php`, `CompanyPolicy.php`, `AppendOnlyPolicy.php`, `AuditLogPolicy.php`
- `resources/js/Pages/Kanban/Board.tsx`
- `resources/js/Pages/Companies/Import/MappingStep.tsx`
- `resources/js/Pages/Admin/Audit/Show.tsx`

## Verificação

1. `php artisan migrate --seed` cria schema + dados de exemplo (empresa, equipes, produtos, usuários admin/manager/consultant, seeds de states/cities/cnaes) sem erro.
2. `php artisan test` cobrindo: transições válidas/inválidas, bloqueio de delete em tabelas append-only, RBAC, dedupe de CNPJ na importação, mapeamento de colunas com cabeçalho fora do padrão, validação de janela ANATEL (gera aviso, não bloqueia), cálculo de fit score, geração de `audit_logs` em create/update/delete de Lead/Company/Proposal/FollowUp com `actor_type` correto (user via request autenticado, import via `ProcessCsvImportJob`, system via Command agendado), e `AuditLogReconstructor::stateAt()` retornando o estado correto em pontos no tempo distintos.
3. Importar manualmente um CSV de amostra (incluindo um com colunas renomeadas/reordenadas) e validar que o wizard de mapeamento detecta automaticamente o que pode e pede confirmação do resto; confirmar que as alterações em `companies` geradas por essa importação aparecem em `audit_logs` com `actor_type=import` e `actor_label` referenciando o `import_batch_id`.
4. Testar no navegador: criar lead a partir de uma company importada, arrastar card pelas colunas até Ganho/Perdido, verificar histórico único no painel lateral, criar follow-up vencido, confirmar alerta de SLA, tentar registrar ligação fora da janela permitida e confirmar que aparece aviso (não bloqueio).
5. Checar `audit_logs`, `lead_stage_history` e leitura de CPF de sócio (deve gerar entrada de auditoria) populados corretamente, sem nenhum registro deletável; como admin/manager, acessar `Admin/Audit/Show` de um Lead editado várias vezes, selecionar uma data/hora intermediária e confirmar que o estado reconstruído bate com o valor real daquele momento (sem alterar o registro atual); confirmar que um usuário `consultant` recebe 403 ao tentar acessar essa tela.
